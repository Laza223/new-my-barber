/**
 * Shop Repository — queries de DB para la barbería.
 * Único punto de contacto con la DB. Sin lógica de negocio.
 */

// TODO: Implementar
// - findByUserId(userId)
// - findById(shopId)
// - update(shopId, data)
// - updateOnboardingStep(shopId, step)
// - markOnboardingComplete(shopId)
