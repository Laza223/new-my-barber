/**
 * Dashboard Repository — queries de DB para reportes.
 * Todas las queries usan agregaciones SQL, no cálculos en Node.
 */

// TODO: Implementar
// - getDailyReport(shopId, date)
// - getWeeklyReport(shopId, weekStart)
// - getMonthlyReport(shopId, month)
// - getYearlyReport(shopId, year)
// - getDateRangeReport(shopId, from, to)
// - getTrends(shopId, period)
// - getPaymentMethodBreakdown(shopId, month)
// - getTopServices(shopId, month, limit)
// - getProfessionalStats(shopId, month)
// - getCommissionsSummary(shopId, month)
