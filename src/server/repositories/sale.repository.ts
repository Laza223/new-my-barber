/**
 * Sale Repository — queries de DB para ventas.
 * Usa índices compuestos (shop_id, business_date) para performance.
 */

// TODO: Implementar
// - create(data) — con .returning()
// - delete(shopId, saleId) — hard delete
// - findByDate(shopId, date)
// - findByRange(shopId, from, to, filters)
// - getDailySummary(shopId, date) — SUM/COUNT/GROUP BY
// - countTodayByShopId(shopId) — para verificar límite FREE
