/**
 * Auth Service — lógica de negocio de autenticación.
 * Se implementa en la fase de auth con Better-Auth.
 */

// TODO: Implementar
// - validateSession(sessionId: string)
// - getCurrentUser()
// - getCurrentShop()
// - requireAuth() — throw si no autenticado
// - requirePlan(plan: PlanId) — throw si plan insuficiente
