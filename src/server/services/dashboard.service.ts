/**
 * Dashboard Service — lógica de negocio de reportes y dashboard.
 * Usa agregaciones SQL (SUM/COUNT/GROUP BY), no cálculos en Node.
 */

// TODO: Implementar
// - getDashboardData(shopId)
// - getDaily(shopId, date)
// - getWeekly(shopId, date)
// - getMonthly(shopId, month)
// - getYearly(shopId, year)
// - getDateRange(shopId, from, to)
// - getTrends(shopId, period)
// - getPaymentMethods(shopId, month)
// - getTopServices(shopId, month, limit)
// - getProfessionalReport(shopId, month)
// - getCommissions(shopId, month)
// - getGoalProgress(shopId, month)
// - getProjections(shopId, month)
