/**
 * Subscription Service — lógica de negocio de suscripciones.
 * Integra con MercadoPago Preapproval.
 */

// TODO: Implementar
// - create(shopId, plan) — crear preapproval en MP
// - cancel(shopId) — cancelar en MP
// - verify(shopId, preapprovalId) — verificar con API de MP
// - handleWebhookEvent(event) — procesar webhook
// - getStatus(shopId)
// - checkTrials() — cron
// - checkPastDue() — cron
