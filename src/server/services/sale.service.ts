/**
 * Sale Service — lógica de negocio de ventas.
 * Core del negocio. Calcula comisiones con aritmética segura.
 */

// TODO: Implementar
// - create(shopId, data) — snapshot precio, calcular comisión
// - delete(shopId, saleId) — solo del día actual
// - listByDate(shopId, date)
// - listByRange(shopId, from, to, filters)
// - getDailySummary(shopId, date) — agregaciones SQL
