/**
 * Service Service — lógica de negocio de servicios.
 * (Sí, el nombre es redundante, pero es la convención del dominio)
 */

// TODO: Implementar
// - create(shopId, data) — verificar max 50
// - update(shopId, serviceId, data)
// - softDelete(shopId, serviceId)
// - listActive(shopId)
// - toggleActive(shopId, serviceId)
// - createBulk(shopId, services[]) — para onboarding
