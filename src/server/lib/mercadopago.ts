/**
 * MercadoPago client.
 * Se configura en la fase de suscripciones.
 *
 * Usa SDK v2 para Preapproval (suscripciones recurrentes).
 */

// TODO: Implementar en fase de suscripciones
// import { MercadoPagoConfig, PreApproval } from 'mercadopago';
//
// const client = new MercadoPagoConfig({
//   accessToken: process.env.MERCADOPAGO_ACCESS_TOKEN!,
// });
//
// export const preapproval = new PreApproval(client);
//
// export function createPreapproval(params: {...}) { ... }
// export function cancelPreapproval(id: string) { ... }
// export function getPreapproval(id: string) { ... }
// export function verifyWebhookSignature(signature: string, body: string) { ... }
