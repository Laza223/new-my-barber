'use server';

/**
 * Server Actions para la barbería (shop).
 * "shop" = la entidad barbería/negocio del usuario.
 */

// TODO: Implementar en fase de onboarding/settings
// - updateShop(formData: FormData) — nombre, teléfono, días laborales
// - updateOnboardingStep(step: number, data: unknown)
// - completeOnboarding()
// - updateMonthlyGoal(goal: number | null)
// - exportMyData()
// - deleteMyAccount()

export async function placeholder() {
  return { success: false, error: 'Not implemented' };
}
