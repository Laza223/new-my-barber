'use server';

/**
 * Server Actions para servicios.
 * CRUD de los servicios que ofrece la barbería.
 */

// TODO: Implementar en fase de servicios
// - createService(formData: FormData)
// - updateService(id: string, formData: FormData)
// - deleteService(id: string) — soft delete
// - toggleServiceActive(id: string)
// - reorderServices(ids: string[])
// - createBulkServices(services: { name: string; priceCents: number }[])

export async function placeholder() {
  return { success: false, error: 'Not implemented' };
}
