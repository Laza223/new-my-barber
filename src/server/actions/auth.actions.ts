'use server';

/**
 * Server Actions para autenticación.
 * Cada función es un Server Action que puede ser llamado
 * directamente desde un Client Component.
 */

// TODO: Implementar en fase de auth
// - signUp(formData: FormData)
// - signIn(formData: FormData)
// - signOut()
// - forgotPassword(formData: FormData)
// - resetPassword(formData: FormData)
// - verifyEmail(token: string)
// - resendVerification()

export async function placeholder() {
  return { success: false, error: 'Not implemented' };
}
