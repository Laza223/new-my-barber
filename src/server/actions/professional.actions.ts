'use server';

/**
 * Server Actions para profesionales.
 * Requiere plan Business.
 */

// TODO: Implementar en fase de profesionales
// - createProfessional(formData: FormData)
// - updateProfessional(id: string, formData: FormData)
// - deleteProfessional(id: string) — soft delete
// - toggleProfessionalActive(id: string)
// - reorderProfessionals(ids: string[])

export async function placeholder() {
  return { success: false, error: 'Not implemented' };
}
