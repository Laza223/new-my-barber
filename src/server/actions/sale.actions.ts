'use server';

/**
 * Server Actions para ventas.
 * El core del negocio — registrar ventas rápidamente.
 */

// TODO: Implementar en fase de ventas
// - createSale(formData: FormData) — calcula comisión + house_amount
// - deleteSale(id: string) — solo del día actual, hard delete
// - getSalesByDate(date: string) — YYYY-MM-DD
// - getDailySummary(date: string)

export async function placeholder() {
  return { success: false, error: 'Not implemented' };
}
